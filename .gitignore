*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
.gstack/

# Salida generada por /graphify (grafo, caché, reporte) — se reconstruye
# en segundos, no es código fuente.
/graphify-out/

# Material fuente (frames/video) que ya no se usa; solo infla el repo.
/IMAGENES

# Herramienta local de túnel; no es parte del proyecto.
/ngrok.exe
